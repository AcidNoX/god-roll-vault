{
  "name": "@god-roll-vault/destiny-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "validate-data": "pnpm build && node dist/god-rolls/validate-data.js",
    "generate-inventory-preview": "tsx src/preview/generate-inventory-preview.ts"
  },
  "dependencies": {
    "@god-roll-vault/core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@god-roll-vault/config": "workspace:*",
    "@god-roll-vault/vitest": "workspace:*",
    "@types/node": "^20.12.0",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
