{
  "name": "@god-roll-vault/destiny-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "validate-data": "pnpm build && node dist/god-rolls/validate-data.js"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@god-roll-vault/config": "workspace:*",
    "@god-roll-vault/vitest": "workspace:*",
    "@types/node": "^20.12.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
